device:
  path: auto        # serial path, or "auto" to find the first Loupedeck CT
  brightness: 0.7   # screen backlight, 0-1

project:
  path: .           # project the Develop page builds/tests; relative to this file

profile:
  name: default
